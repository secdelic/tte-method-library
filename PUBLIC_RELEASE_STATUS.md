# Public release status

`PUBLIC_RELEASE_STATUS = PRIVATE_GITHUB_RC1_PRECOMMIT_VALIDATED_REMOTE_PENDING`

All local scientific, numerical, rights, license, privacy, identifier, Quick Start, figure, table and release-dry-run gates pass. The repository is initialized on `main`, has no remote and contains only a staged prospective root commit. The researcher-confirmed Git identity is configured locally for this repository; global Git configuration was not changed.

The next authorized step is to create the fresh root commit:

```text
Prepare Target Trial Emulation Method Library v1.0.0-rc1
```

Private GitHub creation, the repository URL and GitHub Actions remain remote-stage fields. Release date and Zenodo DOI remain intentionally pending and are outside this RC1 validation task.
